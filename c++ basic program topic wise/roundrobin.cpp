#include <iostream>
using namespace std;

int main()
{
    int process, s, i, j, k, arival[10], bt[10], temp, tq;
    //variable  = process for total process and s for to store process value from the user;
    //i for loop initialization tq =time quantumn
    // bt for brust time temp for storing burst time
    cout << "Enter Total Process\n";
    cin >> process;
    s = process;
    cout << "Enter Time Quantumn For Total Process\n";
    cin >> tq;
    for (i = 0; i < s; ++i)
    {
        cin >> arival[i];
    }
    for (j = 0; j < s; ++j)
    {
        cin >> bt[10];
        temp[j] = bt[i];
    }
    for (i = 0; i < s; ++i)
    {
        if (bt[i] >= tq)

            for (J = 0; j < tq; ++j)
            {
                
            }
    }
}
